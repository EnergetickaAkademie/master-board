#ifndef SECRETS_H
#define SECRETS_H

#include <cstddef>

// WiFi Network Configuration
// Add your WiFi networks in order of preference (highest priority first)
// The system will try each network in order until one connects
struct WiFiNetwork {
    const char* ssid;
    const char* password;
};

static const WiFiNetwork WIFI_NETWORKS[] = {
    // Add more networks as needed
    // {"YourNetwork", "YourPassword"},
    // {"BackupNetwork", "BackupPassword"},
};

static const size_t WIFI_NETWORK_COUNT = sizeof(WIFI_NETWORKS) / sizeof(WIFI_NETWORKS[0]);

// Server Configuration
#define SERVER_USERNAME "board1"
#define SERVER_PASSWORD "board123"

#endif // SECRETS_H
